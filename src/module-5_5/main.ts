// wilecaed import
// import * as Methods from "./module";
// import divide from "./module";
const addFunction = (pram1: number, pram2: number): number => {
  return pram1 + pram2;
};

// const res = Methods.addFunction(1, 2);
// divide(87, 99);
